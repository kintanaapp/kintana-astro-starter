Mirror of the Next.js starter powered by Astro 5 — copy `.env.example` → `.env` before `pnpm dev`.

Uses `@kintana/sdk@^0.9.0`. Gallery and logo come from `getSiteManifest()` — no folder ids or filename conventions. Request-a-show resolves `SHOW_REQUEST` via the API.
